def is_even_number(number):
    """Check if number is even.

    :param int number:
        Any number.
    :returns:
        True if number is even else False.
    :rtype:
        bool
    """
    return number % 2 == 0
